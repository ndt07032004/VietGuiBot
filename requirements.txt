fastapi==0.115.0
uvicorn==0.30.0
langchain==0.2.0
langchain-pinecone==0.1.0
langchain-ollama==0.1.0
pinecone-client==3.2.0
sentence-transformers==2.2.0
whisper==1.1.10  # ASR fallback
SpeechRecognition==3.10.0  # Fallback
webrtcvad==2.0.10  # VAD
facebook-mms-tts-vie==0.1.0  # TTS
torch==2.1.0
python-dotenv==1.0.0
pyaudio==0.2.11
websockets==12.0
redis==5.0.0
transformers==4.35.0  # Cho TTS fallback

## Cấu hình
./system.conf` (mặc định dùng public config).

   `./system.conf` (mặc định dùng public config).